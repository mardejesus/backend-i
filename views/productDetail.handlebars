<h1>{{title}}</h1>
<p>{{description}}</p>
<p>Precio: ${{price}}</p>
<p>Categoría: {{category}}</p>

<form method="POST" action="/api/carts/{{cartId}}/product/{{_id}}">
  <button type="submit">Agregar al carrito</button>
</form>
